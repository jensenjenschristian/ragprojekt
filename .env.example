# Copy this file to `.env` ONLY if you try the optional hosted-API swap.
# The default local (Ollama) path needs none of these.

OPENAI_API_KEY=
# Model overrides (optional)
# EMBED_MODEL=text-embedding-3-small
# CHAT_MODEL=gpt-4o-mini
